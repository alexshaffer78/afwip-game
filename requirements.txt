# AFWIP game — runtime dependencies (no RL training stack, no torch).
numpy==1.26.4
gymnasium==1.1.1
pettingzoo==1.26.1
pydantic>=2.13
fastapi>=0.115
uvicorn>=0.30
onnxruntime>=1.18      # runs the trained AI opponents (models/*.onnx); no torch needed
openai>=1.30           # optional: the "GPT (OpenAI)" opponent (paste your own key in-app)
httpx>=0.27            # FastAPI TestClient transport (running the test suite)
